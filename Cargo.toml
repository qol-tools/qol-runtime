[package]
name = "qol-runtime"
version = "0.1.0"
edition = "2021"
description = "Runtime protocol and client for QoL Tray platform state"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
